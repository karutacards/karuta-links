services:
  app:
    build: .
    ports:
      - "8787:8787"
    volumes:
      - .:/app
      - app_node_modules:/app/node_modules
    environment:
      WRANGLER_SEND_METRICS: "false"
    command: pnpm dev

volumes:
  app_node_modules:
